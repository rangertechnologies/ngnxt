import { DebounceClickDirectiveDirective } from './debounce-click-directive.directive';

describe('DebounceClickDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceClickDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
